pre-commit:
  parallel: false
  jobs:
    - run: npm run lint
      glob: '**/*.{js,ts,jsx,tsx}'

    - run: npm run prettier 
      glob: '**/*.{js,ts,jsx,tsx,json,css,scss,md,html,vue}'
      stage_fixed: true




